# 🔧 Декомпозиция BotsTab.tsx

## Что было сделано

Большой компонент `BotsTab.tsx` (620+ строк) был разделён на 4 независимых компонента для лучшей читаемости и поддержки кода.

## Структура компонентов

### 1. **CreateBotDialog.tsx** 
📁 `src/components/bots/CreateBotDialog.tsx`

**Назначение:** Диалоговое окно для создания нового бота

**Содержит:**
- Форму с полями: название, токен, описание, логика работы
- Выбор типа бота (QR-ключи, магазин, подписки и т.д.)
- Настройки QR-кодов (количество бесплатных/платных)
- Настройку ротации QR-кодов
- Настройку платежной ссылки Т-банка
- Кнопки "Создать" и "Отмена"

**Размер:** ~330 строк

---

### 2. **BotCard.tsx**
📁 `src/components/bots/BotCard.tsx`

**Назначение:** Карточка отдельного бота

**Содержит:**
- Название и тип бота
- Статус (активный, на модерации, отклонён)
- Ссылку на бота в Telegram
- Функции бота (QR-коды, VIP, пользователи)
- Статусные сообщения (модерация, отклонение, одобрение)
- Кнопки "Настройки", "Статистика", "Удалить"

**Размер:** ~170 строк

---

### 3. **BotSettingsDialog.tsx**
📁 `src/components/bots/BotSettingsDialog.tsx`

**Назначение:** Диалоговое окно настроек бота

**Содержит:**
- Чекбокс "Включить платные QR-коды"
- Поле для ввода платежной ссылки Т-банка
- Кнопки "Сохранить" и "Отмена"
- Логику сохранения настроек через API

**Размер:** ~100 строк

---

### 4. **BotStatsDialog.tsx**
📁 `src/components/bots/BotStatsDialog.tsx`

**Назначение:** Диалоговое окно со статистикой бота

**Содержит:**
- Карточки с количеством пользователей и сообщений
- Статистику по QR-кодам (всего, использовано, доступно, VIP, бесплатные)
- Информацию о платежной ссылке (если включена)
- Индикатор загрузки данных
- Обработку ошибок загрузки статистики

**Размер:** ~160 строк

---

### 5. **BotsTab.tsx** (главный компонент)
📁 `src/components/BotsTab.tsx`

**Назначение:** Главный компонент вкладки "Мои боты"

**Содержит:**
- Импорт всех дочерних компонентов
- State-менеджмент для настроек и статистики
- Функции `openSettings()`, `openStats()`, `saveSettings()`
- Функцию `getBotTypeLabel()` для отображения типов ботов
- Layout: заголовок, предупреждение о тарифе, сетку с ботами
- Передачу props в дочерние компоненты

**Размер:** ~280 строк (↓ 340 строк от оригинала)

---

## Преимущества декомпозиции

✅ **Читаемость:** Каждый компонент отвечает за одну задачу  
✅ **Поддержка:** Легче находить и исправлять баги  
✅ **Переиспользование:** Компоненты можно использовать в других местах  
✅ **Тестирование:** Проще писать unit-тесты для небольших компонентов  
✅ **Масштабирование:** Легко добавлять новые функции в отдельные компоненты  

---

## Что НЕ изменилось

🔒 **Логика работы:** Вся бизнес-логика осталась идентичной  
🔒 **API:** Все эндпоинты и запросы работают так же  
🔒 **UI/UX:** Интерфейс выглядит абсолютно идентично  
🔒 **Props:** Все пропсы передаются корректно  
🔒 **State:** State-менеджмент не изменился  

---

## Диаграмма компонентов

```
BotsTab (главный)
│
├── CreateBotDialog (диалог создания)
│   ├── Dialog
│   ├── Input, Label, Textarea
│   ├── Select (тип бота)
│   ├── Checkbox (платежи)
│   └── Button (Создать/Отмена)
│
├── BotCard[] (список карточек ботов)
│   ├── Card
│   ├── Icon (статус, функции)
│   └── Button (Настройки/Статистика/Удалить)
│
├── BotSettingsDialog (диалог настроек)
│   ├── Dialog
│   ├── Checkbox (включить платежи)
│   ├── Input (платежная ссылка)
│   └── Button (Сохранить/Отмена)
│
└── BotStatsDialog (диалог статистики)
    ├── Dialog
    ├── Card[] (пользователи, сообщения)
    ├── Статистика QR-кодов
    └── Button (Закрыть)
```

---

## Как использовать

Главный компонент импортирует все дочерние компоненты автоматически:

```tsx
import BotsTab from '@/components/BotsTab';

// Используйте как обычно
<BotsTab 
  bots={bots}
  newBotName={newBotName}
  // ... остальные props
/>
```

---

## Файловая структура

```
src/components/
├── BotsTab.tsx               (главный компонент)
└── bots/
    ├── CreateBotDialog.tsx   (диалог создания)
    ├── BotCard.tsx           (карточка бота)
    ├── BotSettingsDialog.tsx (настройки)
    └── BotStatsDialog.tsx    (статистика)
```

---

**Дата рефакторинга:** 15 января 2025  
**Версия:** 2.1  
**Статус:** ✅ Готово и протестировано
